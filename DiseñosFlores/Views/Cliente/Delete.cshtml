@model Cliente

<h2>Eliminar Cliente</h2>

<p>¿Seguro que quieres eliminar @Model.RazonSocial?</p>

<form asp-action="Delete">
    <input type="hidden" asp-for="IdCliente" />
    <button type="submit">Eliminar</button>
</form>
